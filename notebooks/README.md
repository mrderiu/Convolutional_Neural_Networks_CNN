# Notebooks

`HabibDeriu_AC2M6_original.ipynb` se conserva como referencia del trabajo experimental.

El código que debe considerarse fuente de verdad para entrenamiento, evaluación e
inferencia está en `src/`.
